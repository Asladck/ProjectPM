# 🚀 SDU Platform - Быстрый старт

## Запуск всех сервисов одной командой

### Windows:

Дважды кликните на файл:
```
start-all.bat
```

Этот скрипт автоматически запустит:
- ✅ Java Assignment Service (порт 4062)
- ✅ Java Attendance Service (порт 4061)  
- ✅ Go Backend + WebSocket (порты 9090, 8085, 50051)

### Или запустите сервисы по отдельности:

#### 1. Java Assignment Service (порт 4062)
```
start-assignment-service.bat
```

#### 2. Java Attendance Service (порт 4061)
```
start-attendance-service.bat
```

#### 3. Go Backend (порты 9090, 8085, 50051)
```
start-go-backend.bat
```

---

## Проверка статуса сервисов

Запустите:
```
check-services.bat
```

---

## Доступ к приложению

После запуска всех сервисов откройте браузер:
```
http://localhost:8085
```

---

## Структура портов

| Сервис | Порт | URL |
|--------|------|-----|
| Frontend + WebSocket | 8085 | http://localhost:8085 |
| Go Auth API | 9090 | http://localhost:9090 |
| Java Attendance | 4061 | http://localhost:4061 |
| Java Assignment | 4062 | http://localhost:4062 |
| gRPC | 50051 | localhost:50051 |

---

## Остановка сервисов

Закройте окна командной строки с запущенными сервисами.

---

## Альтернатива: Docker Compose

Если у вас установлен Docker:

```bash
docker-compose up --build
```

Все сервисы запустятся автоматически в контейнерах.

---

## Troubleshooting

### Если порт занят:

Найдите процесс:
```
netstat -ano | findstr :4062
```

Завершите процесс:
```
taskkill /PID <номер_процесса> /F
```

### Если Maven не найден:

Убедитесь что `mvnw.cmd` исполняемый:
```
cd java\inf451\file_upload-download
mvnw.cmd --version
```

---

## Требования

- Java 21+
- Go 1.25+
- Maven (встроен в проект через wrapper)
- PostgreSQL (запустится автоматически через Docker или нужен локальный)
- MongoDB (для file upload service)

---

🎉 **Готово! Приложение работает на http://localhost:8085**

