# 🧹 Очистка дублирующихся папок

## Проблема

В корне проекта есть старые папки `css/`, `js/` и возможно `public/`, которые дублируют содержимое из `web/`. 

Все фронтенд файлы должны быть ТОЛЬКО в папке `web/`.

## Решение

Запустите один из этих скриптов:

### Вариант 1: PowerShell (рекомендуется)
```powershell
# Из корня проекта
.\cleanup.ps1
```

### Вариант 2: Batch файл
```cmd
cleanup-duplicates.bat
```

### Вариант 3: Вручную
Удалите папки:
- `C:\Users\DWA\GolandProjects\Pjpro\css\`
- `C:\Users\DWA\GolandProjects\Pjpro\js\`
- `C:\Users\DWA\GolandProjects\Pjpro\public\` (если есть)

## Проверка

После очистки должна быть такая структура:

```
Pjpro/
├── cmd/
├── configs/
├── internal/
├── java/
├── web/              ✅ ВСЁ ЗДЕСЬ
│   ├── css/          ✅
│   ├── js/           ✅
│   └── public/       ✅
├── go.mod
└── ...
```

В корне НЕ должно быть:
- ❌ css/
- ❌ js/
- ❌ public/

## После очистки

Фронтенд будет работать как раньше, потому что все HTML файлы уже используют правильные относительные пути (`../css/main.css`, `../js/api.js`).

Запуск:
```bash
cd web
python -m http.server 8000
# http://localhost:8000/public/login.html
```

Готово! 🎉

