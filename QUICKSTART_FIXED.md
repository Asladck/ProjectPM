# 🚀 Quick Start Guide

## Проблема была решена!

### Что было исправлено:

1. ✅ **Пути к CSS/JS файлам** - изменены с абсолютных (`/css/main.css`) на относительные (`../css/main.css`)
2. ✅ **Навигационные ссылки** - обновлены на относительные пути с `.html` расширением
3. ✅ **Автоматическая проверка auth** - удалена из `api.js` (конфликтовала с относительными путями)
4. ✅ **Редиректы после login** - исправлены на относительные пути
5. ✅ **Значения ролей** - изменены на UPPERCASE (`STUDENT`, `TEACHER`)

---

## 📍 Как запустить (2 простых шага):

### Шаг 1: Запустить backend сервисы

```bash
# Из корня проекта (C:\Users\DWA\GolandProjects\Pjpro)
docker-compose up --build
```

Подождите пока все сервисы запустятся. Проверьте:
- ✅ Go backend: http://localhost:9090
- ✅ Attendance service: http://localhost:4061
- ✅ File service: http://localhost:4062
- ✅ WebSocket: ws://localhost:8085

### Шаг 2: Запустить frontend

**Вариант A (рекомендуется):**
```bash
cd web
python -m http.server 8000
```

**Вариант B (автоматически):**
Запустите `start-platform.bat` из корня проекта

### Шаг 3: Открыть браузер

Откройте:
```
http://localhost:8000/public/login.html
```

---

## 🧪 Тестирование

### 1. Создать тестового пользователя (через curl или Postman):

**Teacher:**
```bash
curl -X POST http://localhost:9090/auth/sign-up \
  -H "Content-Type: application/json" \
  -d "{\"name\":\"John\",\"surname\":\"Doe\",\"email\":\"teacher@test.com\",\"password\":\"123456\",\"role\":\"TEACHER\"}"
```

**Student:**
```bash
curl -X POST http://localhost:9090/auth/sign-up \
  -H "Content-Type: application/json" \
  -d "{\"name\":\"Jane\",\"surname\":\"Smith\",\"email\":\"student@test.com\",\"password\":\"123456\",\"role\":\"STUDENT\"}"
```

### 2. Войти в систему:

1. Откройте http://localhost:8000/public/login.html
2. Email: `teacher@test.com`
3. Password: `123456`
4. Role: `Teacher`
5. Нажмите Sign In

**Результат:** автоматический редирект на `teacher-dashboard.html`

### 3. Проверить функции:

**Как Teacher:**
- ✅ Создать задание (Assignments → Create Assignment)
- ✅ Просмотреть задания
- ✅ Управлять посещаемостью (Attendance)
- ✅ Использовать чат (Chat)

**Как Student:**
- ✅ Просмотреть задания
- ✅ Загрузить файл задания
- ✅ Просмотреть посещаемость
- ✅ Использовать чат

---

## ❗ Важно

### Правильный запуск сервера:

**✅ ПРАВИЛЬНО:**
```bash
cd web
python -m http.server 8000
# Откройте: http://localhost:8000/public/login.html
```

**❌ НЕПРАВИЛЬНО:**
```bash
# НЕ запускайте из корня проекта!
python -m http.server 8000
# Это не сработает с относительными путями
```

### Структура URL:

```
http://localhost:8000/public/login.html          ✅ ПРАВИЛЬНО
http://localhost:8000/public/teacher-dashboard.html  ✅ ПРАВИЛЬНО
http://localhost:8000/public/chat.html           ✅ ПРАВИЛЬНО

http://localhost:8000/login.html                 ❌ НЕ РАБОТАЕТ
file:///C:/Users/.../login.html                  ❌ НЕ РАБОТАЕТ
```

---

## 🐛 Troubleshooting

### Проблема: "auth is not defined"

**Причина:** Скрипты не загружаются
**Решение:** 
1. Убедитесь, что сервер запущен из папки `web`
2. Проверьте DevTools → Network → убедитесь что `api.js` и `auth.js` загружаются с 200

### Проблема: "Failed to fetch"

**Причина:** Backend не запущен
**Решение:**
```bash
docker-compose up --build
docker-compose logs app
```

### Проблема: Редирект не работает после login

**Причина:** Роль не соответствует
**Решение:** Убедитесь, что:
- В форме login выбрана роль: `Student` или `Teacher`
- В localStorage роль сохранена как `STUDENT` или `TEACHER` (uppercase)

Проверить в console:
```javascript
localStorage.getItem('role')  // Должно быть TEACHER или STUDENT
localStorage.getItem('token')  // Должен быть токен
```

### Проблема: WebSocket не подключается

**Причина:** Go backend не запущен или нет токена
**Решение:**
1. Проверьте что Go backend работает на порту 8085
2. Проверьте что есть access token в localStorage
3. Откройте console и смотрите ошибки WebSocket

---

## 📂 Файлы которые были изменены:

```
✅ web/public/login.html
✅ web/public/teacher-dashboard.html
✅ web/public/student-dashboard.html
✅ web/public/assignment-teacher.html
✅ web/public/assignment-student.html
✅ web/public/attendance-teacher.html
✅ web/public/attendance-student.html
✅ web/public/chat.html
✅ web/js/api.js
✅ web/js/auth.js
```

Все пути изменены на относительные, все редиректы исправлены!

---

## ✅ Checklist готовности:

- [ ] Backend запущен (`docker-compose up`)
- [ ] Frontend сервер запущен из `web/` папки
- [ ] Открыт браузер на `http://localhost:8000/public/login.html`
- [ ] Создан тестовый пользователь
- [ ] Успешный вход в систему
- [ ] Редирект на dashboard работает
- [ ] Все функции доступны

---

## 🎉 Готово!

Теперь всё должно работать! Если возникнут проблемы:

1. Проверьте Browser Console (F12)
2. Проверьте Network tab
3. Проверьте `docker-compose logs`
4. Убедитесь что сервер запущен из папки `web`

**Enjoy! 🚀**

